# ZT Suite Backend Configuration
# Copy this file to .env and update values

# Application
DEBUG=true
APP_NAME=ZT Suite API

# Database
DATABASE_URL=sqlite:///./zt_suite.db

# JWT Configuration
# IMPORTANT: Change this in production!
SECRET_KEY=your-super-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# MFA Configuration
MFA_ISSUER_NAME=ZT Suite
TRUSTED_DEVICE_EXPIRE_DAYS=30
TRUSTED_DEVICE_COOKIE_NAME=trusted_device_token
TRUSTED_DEVICE_COOKIE_SECURE=false
TRUSTED_DEVICE_COOKIE_SAMESITE=lax

# First Admin User (created on startup if no users exist)
FIRST_ADMIN_EMAIL=admin@example.com
FIRST_ADMIN_PASSWORD=changeme123
